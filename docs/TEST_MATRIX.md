# Functional test matrix

This matrix maps product actions and important combinations to automated evidence. Real-browser
store QA remains necessary because YouTube and browser UI cannot be fully represented by jsdom.

| Area              | Cases covered                                                                                                                                                                                                                         | Automated evidence                                                                                                                                   |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| A/B selection     | Set from playback time, numeric input, ±0.1 adjustment, floating-point normalization, legacy long decimals, empty input, invalid and too-short ranges, duration overflow                                                              | `loop-engine-complete`, `bookmarks`, `popup.integration`, `panel.integration`, `controller.integration`                                              |
| Loop playback     | Activate, stop, jump at B, time-update fallback, frame callback, animation-frame fallback, cancellation and stale callbacks                                                                                                           | `loop-engine`, `loop-engine-complete`, `controller.integration`                                                                                      |
| Speed and pitch   | Clamp 0.25×–4×, reject non-finite values, propagate external YouTube rate changes, block changes during ads, always preserve pitch                                                                                                    | `loop-engine-complete`, `controller.integration`, `popup.integration`                                                                                |
| Advertisements    | Block seek/rate mutation, show spinner state, hide sharing controls, restore settings after the ad                                                                                                                                    | `loop-engine`, `panel.integration`, `controller.integration`, `e2e/product-flows`                                                                    |
| Widget            | Initial collapse, automatic first-point expansion, manual expand/collapse, dismiss, restore, replace, destroy and status feedback                                                                                                     | `panel-collapse`, `panel.integration`, `controller.integration`                                                                                      |
| Shortcuts         | Option/Alt+Shift+A/B/L, repeat rejection, unrelated key rejection and input-field isolation                                                                                                                                           | `panel.integration`, `controller.integration`                                                                                                        |
| Saving            | Autogenerated name, reviewed custom name, root/folder destination, duplicate prevention and saved badge                                                                                                                               | `bookmarks`, `popup.integration`, `controller.integration`, `e2e/product-flows`                                                                      |
| Updating          | Rename, A/B/rate edit, clamp, invalid/maximum rejection, duplicate rejection, quick update, cosmetic-name independence and field-level stale-editor merging                                                                           | `bookmarks`, `popup.integration`, `storage-coordinator`, `controller.integration`, `e2e/product-flows`                                               |
| Detaching         | Unlink a saved loop without deletion and prevent accidental reattachment/duplication                                                                                                                                                  | `controller.integration`                                                                                                                             |
| Library folders   | Immediate root rendering, stalled tab/storage recovery, nested creation, rapid-submit deduplication, blank/cancel flows, collapse/expand, deletion, cycle repair and safe child/bookmark reparenting                                  | `bookmarks`, `popup.integration`, `storage.complete`, `storage-coordinator`, `e2e/product-flows`                                                     |
| Drag and drop     | Threshold activation, valid root drop, cancellation and irrelevant pointer gestures                                                                                                                                                   | `popup.integration`                                                                                                                                  |
| Opening fragments | Canonical tab URL, seek to A, bookmark selection, automatic activation, ad/replaced-player survival and play rejection fallback                                                                                                       | `bookmarks`, `popup.integration`, `controller.integration`                                                                                           |
| Shared links      | V2 codec, V1 compatibility, extra-field tolerance, query/fragment parsing, malformed/oversized/foreign rejection, ad/replaced-player survival and clipboard fallback                                                                  | `loop-links`, `early-capture`, `platform.integration`, `popup.integration`, `controller.integration`                                                 |
| Storage           | V1/V2/V3 migration, durable split-layout marker, deletion propagation, stale-snapshot prevention, atomic duplicates, concurrent tabs/windows, stale races, restart/timeout, failed-read protection, ordered writes and 250-loop bound | `storage-slices`, `storage.complete`, `storage-coordinator`, `background.integration`, `popup.integration`, `panel.integration`, `e2e/product-flows` |
| YouTube lifecycle | Watch-page discovery, missing DOM, SPA events, debounce, mutation discovery, navigation detach and controller teardown                                                                                                                | `youtube`, `youtube.integration`, `controller.integration`                                                                                           |
| Localization      | English/Spanish key and placeholder parity, browser translation, fallback strings and localized attributes                                                                                                                            | `locales`, `platform.integration`                                                                                                                    |
| Accessibility     | Popup semantics and shadow-DOM widget checked with axe-core; keyboard isolation and labels exercised                                                                                                                                  | `accessibility`, `panel.integration`, `e2e/product-flows`                                                                                            |
| Packaging         | Manifest parity, compatibility floors, least permissions, deterministic builds, exact-candidate E2E/package handoff, three validators, release rehearsal/source archive and native Chromium MV3 worker                                | `manifests`, `release-tooling`, `lint:extension`, `reproducible`, `release:rehearse`, `e2e/product-flows`                                            |
| Marketplace       | Opaque image dimensions, visual-source freshness, localized summaries, privacy policy and Chrome promotional sizes                                                                                                                    | `store:validate`                                                                                                                                     |
| Performance       | Minified production builds, per-entry/package budgets, loop/codec/1,000-bookmark stress and linear 10,000-folder normalization                                                                                                        | `size`, `perf`                                                                                                                                       |

## Manual release smoke test

For each browser, load a clean profile and verify: a fresh video starts collapsed; A/B/L shortcuts
work; typing does not trigger YouTube shortcuts; an ad swaps controls for the spinner; saving,
renaming, updating, detaching, dragging and deleting survive a restart; a shared link opens and
activates; two simultaneous tabs keep independent playback while library changes become visible in
both; concurrent saves do not duplicate a fragment; SPA navigation leaves one widget;
fullscreen/theater mode stays usable; and no unexpected console error appears.
